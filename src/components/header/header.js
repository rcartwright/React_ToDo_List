var React = require('react');

    var header = React.createClass({
      render: function(){
        return (
          <h3>This is the header</h3>
        );
      }
    });

    module.exports = header;